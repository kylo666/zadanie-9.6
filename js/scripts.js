var list = document.getElementById('list')

add.addEventListener('click' , function('add') {
});

list.addEventListener('click' , function('list') {
});
element.innerHTML = 'li';

console.log(element[li].innerText);

list.appendChild(element);
